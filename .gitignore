node_modules
.next
out
coverage
*.log
*.tsbuildinfo
next-env.d.ts

# Secrets: every env file except the documented template.
.env
.env.*
!.env.example

# The data store: menu, accounts, sessions, audit log and uploaded imagery. Runtime state, kept
# in a Docker volume in production and never committed.
data/
